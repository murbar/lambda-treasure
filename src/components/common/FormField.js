import styled from 'styled-components';

export default styled.div`
  display: flex;
  align-items: center;
  margin: 1rem 0;
  label {
    padding: 0 1rem;
    text-transform: uppercase;
    font-weight: bold;
  }
  input {
    flex-grow: 1;
  }
`;
